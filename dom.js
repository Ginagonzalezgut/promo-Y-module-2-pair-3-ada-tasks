// const list2 = document.querySelector(".js-list2");

// const liElement = document.createElement("li");
// liElement.setAttribute("class", "checked");
// list2.appendChild(liElement);
// console.log(list);
// const input = document.createElement("input");
// liElement.appendChild(input);
// input.setAttribute("class", "js-checkbox");
// input.setAttribute("type", "checkbox");
// input.setAttribute("id", "${task.id}");
// console.log(list);

// const li = document.createElement("li");
// li.setAttribute("class", "checked");
// list.appendChild(li);

// const input = document.createElement("input");
// input.setAttribute("class", "js-checkbox checked");
// input.setAttribute("type", "checkbox");
// input.setAttribute("id", "${task.id}");
// li.appendChild(input);
// const textSpan = document.createTextNode("${task.name}");
// input.appendChild(textSpan);
// console.log(li);

// const input = document.createElement("input");
// input.setAttribute("class", "js-checkbox");
// input.setAttribute("type", "checkbox");
// input.setAttribute("id", "${task.id}");
// li.appendChild(input);

// list.innerHTML += `<li class="checked"><input class="js-checkbox" checked type="checkbox" id="${task.id}"/>${task.name}
// </li>`;
